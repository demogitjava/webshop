create table WEBTEXTLAYOUT
(
    ID        INT not null
        primary key,
    FRAMENAME VARCHAR(255),
    DE        VARCHAR(255),
    ENG       VARCHAR(255),
    FR        VARCHAR(255),
    ESP       VARCHAR(255),
    IT        VARCHAR(255),
    TR        VARCHAR(255),
    BINDTO    VARCHAR(255)
);

